name: docker

on:
  schedule:
  - cron:  '0 3 * * 1'
  push:
    branches: 
      - master

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:

    - name: clone
      uses: actions/checkout@v2

    - name: docker login
      env:
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
      run: echo $DOCKER_TOKEN | docker login --username "$DOCKER_USER" --password-stdin

    - name: docker build
      run: docker build cron -t aczigler/cron:latest
        
    - name: docker push
      run: docker push aczigler/cron:latest

  node-full-icu:
    runs-on: ubuntu-latest
    steps:

    - name: clone
      uses: actions/checkout@v2

    - name: docker login
      env:
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
      run: echo $DOCKER_TOKEN | docker login --username "$DOCKER_USER" --password-stdin

    - name: docker build
      run: docker build node-full-icu -t aczigler/node-full-icu:latest
        
    - name: docker push
      run: docker push aczigler/node-full-icu:latest

  rethink-backup:
    runs-on: ubuntu-latest
    steps:

    - name: clone
      uses: actions/checkout@v2

    - name: docker login
      env:
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
      run: echo $DOCKER_TOKEN | docker login --username "$DOCKER_USER" --password-stdin

    - name: docker build
      run: docker build rethink-backup -t aczigler/rethink-backup:latest
        
    - name: docker push
      run: docker push aczigler/rethink-backup:latest
  
  syncthing:
    runs-on: ubuntu-latest
    steps:

    - name: clone
      uses: actions/checkout@v2

    - name: docker login
      env:
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
      run: echo $DOCKER_TOKEN | docker login --username "$DOCKER_USER" --password-stdin

    - name: docker build
      run: docker build syncthing -t aczigler/syncthing:latest
        
    - name: docker push
      run: docker push aczigler/syncthing:latest